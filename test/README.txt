
You can call TestMatFile on a .mat file to display all variables and their types.
In addition, casting to a concrete type is demonstrated by extracting all two-dimensional double arrays.

The test TestUniversal achieves the same, but by first converting to a unityped representation.
